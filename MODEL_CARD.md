# 🧬 شناسنامه و مشخصات فنی مدل هوش مصنوعی Salinex AI
**نسخه سیستم:** V3.9.5 Adaptive ML Engine  
**توسعه‌دهنده اصلی:** مهندس میرزایی  
**وضعیت عملیاتی:** فعال و تاییدشده (Certified Production)  

---

## 🎯 ۱. هدف و دامنه کاربرد (Model Scope)
- **کاربرد اصلی:** پیش‌بینی آنلاین و علمی درصد بازیافت لیتیوم (Lithium Recovery Rate %) و محاسبه خودکار شاخص اطمینان (Confidence Score)
- **ورودی‌ها (Inputs):** پارامترهای شیمیایی شورابه شامل `Li_ppm`, `Mg_ppm`, `Ca_ppm`, `TDS_ppm` (به همراه فرض یا دریافت آنلاین `Temperature`)
- **خروجی‌ها (Outputs):** درصد بازیافت پیوسته (Recovery Rate %) + بازه اطمینان پیش‌بینی (Confidence Interval)

---

## ⚙️ ۲. معماری، الگوریتم‌ها و پیچیدگی (Model Architecture)
- **نوع مسئله ML:** رگرسیون نظارت‌شده (Supervised Ensemble Regression)
- **الگوریتم‌های پایه (Base Estimators):**
  1. **Random Forest Regressor:** 
     - تعداد درخت‌ها (`n_estimators`): ۱۰۰
     - حداکثر عمق (`max_depth`): ۸
     - مجموع گره‌های تصمیم‌گیری (Total Decision Nodes): **۱۶,۴۷۰ گره**
  2. **Gradient Boosting Regressor:**
     - تعداد درخت‌ها (`n_estimators`): ۱۰۰
     - حداکثر عمق (`max_depth`): ۵
- **مکانیسم ترکیب (Adaptive Ensemble):** میانگین‌گیری وزن‌دار تطبیقی بر اساس نوسانات انحراف معیار پیش‌بینی‌ها (Variance-based Confidence Scoring)

---

## 📊 ۳. ارزیابی دقت و عملکرد (Metrics & Performance)
بر اساس متادیتای ارزیابی مدل نهایی روی داده‌های آزمایشی (Test Dataset):
- **ضریب تعیین ($R^2$ Score):** `0.9975` (دقت ۹۹.۷۵٪)
- **جذر میانگین مربعات خطا (RMSE):** `0.7519%` (میانگین خطای پیش‌بینی زیر ۱ درصد)

---

## 🎯 ۴. درصد اهمیت ویژگی‌ها (Feature Importances)
تأثیر دقیق هر ویژگی در تصمیم‌گیری درخت‌های مدل:
- **`Li_ppm` (غلظت لیتیوم):** **۹۶.۲۸٪** (پارامتر اصلی تعیین‌کننده بازیافت)
- **`Mg_ppm` (غلظت منیزیم):** **۳.۶۴٪** (پارامتر مزاحم / رقابتی اصلی)
- **`TDS_ppm` (جامدات محلول):** **۰.۰۶٪**
- **`Ca_ppm` (غلظت کلسیم):** **۰.۰۲٪**

---

## 📊 ۵. میانگین و انحراف معیار داده‌های آموزش (Training Data Distribution)
داده‌ها با مقادیر زیر نرمال‌سازی و مقیاس‌گذاری شده‌اند:
- **`Li_ppm`:** میانگین $34.61$ | انحراف معیار $48.58$
- **`Mg_ppm`:** میانگین $1707.74$ | انحراف معیار $1365.45$
- **`Ca_ppm`:** میانگین $1106.31$ | انحراف معیار $1372.21$
- **`TDS_ppm`:** میانگین $43728.00$ | انحراف معیار $35987.55$

---

## 💾 ۶. وضعیت فایل‌ها و حجم آرتیفکت‌ها (Model Artifacts)
- **`rf_model.pkl`:** ۱,۱۹۰ KB (مدل Random Forest)
- **`gb_model.pkl`:** ۲۹۲ KB (مدل Gradient Boosting)
- **`brine_model.pkl`:** ۷,۲۷۶ KB (مدل پایه / پشتیبان ۵ متغیره)
- **`model_metadata.pkl`:** ۰.۳۱ KB (تنظیمات، ضریب $R^2$ و انحراف معیارها)